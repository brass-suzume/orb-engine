#pragma once

namespace core
{
	/////////////////////////////////////////////////////////////////////////////////////////////
	//
	enum class RenderLayer
	{
		Invalid = -1,
		Background,
		World,
		UI,

		_COUNT,
	};


	/////////////////////////////////////////////////////////////////////////////////////////////
	//
	using RenderProxyID = uint16_t;

	constexpr RenderProxyID c_invalidRenderProxyID = 0;
}